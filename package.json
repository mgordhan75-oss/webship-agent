{
  "name": "webship-agent",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npx playwright install chromium && npx playwright install-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.40.0"
  }
}